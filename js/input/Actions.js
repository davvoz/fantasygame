/**
 * Abstract action identifiers decoupled from physical keys.
 * Add new actions here as the game grows.
 */
export const Actions = Object.freeze({
    MOVE_LEFT:  'MOVE_LEFT',
    MOVE_RIGHT: 'MOVE_RIGHT',
    SPELL:      'SPELL',
    JUMP:       'JUMP',
    CROUCH:     'CROUCH',
    BLOCK:      'BLOCK',
});
